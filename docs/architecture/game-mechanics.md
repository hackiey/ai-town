# 游戏机制百科

本文是一份面向玩家的当前版本机制说明，按实际可游玩的规则整理。重点不是“设计目标”，而是“现在这版游戏里到底怎么运作”。

---

## 1. 当前版本概览

这一版的核心玩法是：

1. 在小镇中移动、观察和与 NPC 交流
2. 打开背包管理材料、食物、工具和容器
3. 在工作台链路里把原料加工成工具、食物和中间件
4. 在农田里播种、浇水、除虫、收获
5. 用自然语言给自己的角色下达高层指令

当前世界内容量级：

- NPC：25 名
- 农田：10 片
- 农田格数总计：303 格
- 工作站节点：19 个
- 工作站种类：9 种
- 水井数量：6 口

NPC 分布大致如下：

- Vale 农户 3 人
- Greystone 农户 5 人
- Saint Candle 教会 2 人
- Saint Bell 教会 3 人
- 矿工 3 人
- 王室内务采办 1 人
- 磨坊 / 面包店 2 人
- 铁矿 / 炭窑 / 伐木场 / 杂货店 / 铁匠铺 6 人

---

## 2. 基本操作

当前确认可用的操作键和指令如下：

| 操作 | 说明 |
|---|---|
| `E` | 与工作站或农场面板交互 |
| `B` | 打开 / 关闭背包 |
| `ESC` | 关闭当前面板 |
| 背包右键 | 打开“使用 / 丢弃”菜单 |
| 拖拽 | 背包内换位，或把物品拖进工作台 / 农场 |
| 普通输入 | 对附近说话 |
| `/command ...` | 把自然语言命令发给后台代理 |

当前内置 slash 指令：

| 指令 | 作用 |
|---|---|
| `/eat <slot>` | 吃掉背包某一格里的食物 |
| `/plant <item_id>` | 向面前空农田种下一份可种植作物 |
| `/water` | 给面前作物浇水 |
| `/pest` | 给面前作物除虫 |
| `/harvest` | 收获面前成熟作物 |
| `/farmtest [seed_id]` | 测试用的批量种植命令 |
| `/timewarp <倍率>` | 改变时间倍率，偏调试用途 |
| `/give ...` | 调试发物品 |

重要说明：

- **食物真正生效的吃法是 `/eat <slot>`。**
- 背包右键的“使用”目前只会消耗 1 个物品，不会自动触发食物效果。

---

## 3. 时间系统

游戏时间持续流动，且明显快于现实时间。

### 3.1 时间倍率

- `1` 现实秒 = `7` 游戏秒
- `1` 现实分钟 = `7` 游戏分钟
- `1` 游戏小时 = 约 `8 分 34 秒` 现实时间
- `1` 游戏天 = 24 游戏小时 = 约 `3 小时 25 分 43 秒` 现实时间
- `1` 游戏年 = `360` 游戏天

### 3.2 时间会推动什么

每过 1 个游戏小时，世界会统一推进一次：

- 作物生长
- 作物水分下降
- 作物照料评分结算
- 农田虫害判定
- 会腐烂的物品老化
- 角色的慢速状态结算

### 3.3 现在版本里时间最重要的影响

- 制作有持续时间
- 农事动作有持续时间
- 作物成熟要等时间
- 会腐烂的食物会随时间降鲜

---

## 4. 角色状态

玩家 HUD 目前显示 4 条主数值：

| 数值 | 上限 | 初始值 | 当前作用 |
|---|---|---|---|
| HP | 100 | 100 | 生命值 |
| 体力 | 100 | 约 50-100 | 会自然恢复，但上限受饱食 / 精力限制 |
| 饱食 | 100 | 约 40-55 | 会随时间下降，吃食物恢复 |
| 精力 | 100 | 按醒来时间约 70-100 | 清醒时下降，睡觉恢复 |

初始值由 DB seed 写入 SQLite，再由 Godot hydrate：服务器从 `统治五年，3月4日 周二 06:00` 开服，角色尚未吃早餐；每个 NPC 在 `backend/data/town/npcs.json` 里有 `initial_wake_time`，醒来时间不早于 6 点。已到醒来时间的人开服时清醒，晚起的人会继续睡到配置时间。已有持久化角色状态会覆盖这组默认初始值。

### 4.1 当前版本的“饥饿”状态

- 饱食 `<= 50` 时会挂上“饥饿”标签
- 饱食 `>= 70` 时会移除“饥饿”标签
- 饥饿状态会在角色面板中显示影响：降低体力上限和恢复速度

### 4.2 当前版本的重要限制

这版的角色数值系统还没有完全展开，现状如下：

- 饱食非线性下降：`100 → 50` 约 4 小时，`50 → 30` 约 4 小时，低于 `30` 后下降明显放慢
- 角色生理 tick 每 `10` 游戏分钟结算一次；饱食、精力、体力恢复和饿死扣血在同一个 physiology 机制里处理
- 清醒时精力每游戏小时 `-2`
- 睡觉时精力按角色所需睡眠小时恢复
- 体力上限受饱食和精力限制；饱食 `50` 时饥饿侧体力上限为 `70`
- 体力低于上限时会自然恢复，标准速度为每 `10` 游戏分钟 `+5`（每游戏小时最多约 `+30`）；饥饿只降低恢复速度和上限，不放大行动体力消耗
- 普通制作目前仍不会实际扣体力；农事会按动作扣体力
- HP 只有在饱食降到 `0` 时才会按每游戏小时 `-2` 掉血

### 4.3 移动性能

- 玩家 / NPC 移动速度：`3.0`
- 面前交互距离（种植 / 浇水 / 除虫 / 收获）：约 `1.5` 米

---

## 5. 背包与物品

### 5.1 背包容量

- 背包固定 `20` 格
- 布局为 `5 x 4`
- 默认最大堆叠数为 `99`

### 5.2 物品什么时候能堆叠

两个物品只有在以下内容完全一致时才会堆到一起：

- `item_id`
- 品质
- 形状
- 材质组成
- 标签
- 物品属性
- 鲜度等级

因此下面这些情况通常不会堆叠：

- 品质不同
- 鲜度不同
- 容器内容不同
- 材质不同

### 5.3 品质分级

品质使用 `1-100` 的整数。

| 品质区间 | 等级 |
|---|---|
| 90-100 | 极品 |
| 70-89 | 优 |
| 40-69 | 普通 |
| 1-39 | 次品 |

品质倍率是**线性**的：

- `100` 品质 = `1.00x`
- `70` 品质 = `0.70x`
- `40` 品质 = `0.40x`

它会直接影响：

- 食物恢复量
- 作物收获量
- 制作结果品质

### 5.4 当前起始背包

新角色首次进入世界时，默认会获得：

| 物品 | 数量 |
|---|---|
| 铁矿石 | 5 |
| 铜矿石 | 3 |
| 锡矿石 | 3 |
| 木炭 | 8 |
| 木材 | 5 |
| 纤维 | 6 |
| 小麦 | 5 |
| 生肉 | 2 |
| 蛋 | 2 |
| 浆果 | 5 |
| 盐 | 3 |
| 番茄种子 | 3 |
| 木桶 | 1 |

注意：

- 起始包里**没有现成的水**
- 你需要带着木桶去水井打水
- 起始包里的 `小麦` 既能直接播种，也能拿去磨坊磨成面粉
- 当前可种植物的判断规则是：物品 `tags` 中包含 `seed`，并且定义了对应的 `crop_variety_id`

---

## 6. 食物、恢复与腐烂

### 6.1 当前可食用食物

以下数值按**100 品质**计算。实际恢复量 = 表中数值 × 品质倍率。

| 食物 | 单次恢复饱食 | 单次恢复体力 | 最大堆叠 |
|---|---|---|---|
| 浆果 | 5 | 0 | 99 |
| 浆果酱 | 15 | 5 | 30 |
| 面包 | 30 | 5 | 99 |
| 熟肉 | 22 | 8 | 50 |
| 腌肉 | 30 | 10 | 50 |
| 蛋饼 | 20 | 15 | 50 |
| 咸蛋饼 | 24 | 9 | 50 |
| 蔬菜炖菜 | 25 | 8 | 20 |
| 盐渍菜汤 | 28 | 9 | 50 |
| 番茄 | 30 | 5 | 99 |

例子：

- 100 品质面包：饱食 `+30`，体力 `+5`
- 70 品质面包：饱食 `+21`，体力 `+3.5`
- 40 品质熟肉：饱食 `+8.8`，体力 `+3.2`

### 6.2 哪些原料暂时不能直接吃

虽然有些材料带“可生食”标签，但当前真正能通过 `/eat` 生效的，仍然要看物品本身是否是 `food`。

因此当前这些原料**不能直接吃**：

- 生肉
- 蛋
- 面团

### 6.3 腐烂规则

带保鲜配置的食物共有 `5` 个鲜度阶段：

1. 新鲜
2. 良好
3. 一般
4. 陈旧
5. 将腐

降到第 0 阶时，整堆物品会直接变成“腐烂食物”。

腐烂后：

- 品质变为 `0`
- 物品会被替换成“腐烂食物”
- 不再按正常食物使用

### 6.4 当前已配置的保鲜时长

下表是**总保鲜时长**，单位都是“游戏小时”。

| 物品 / 材料 | 总保鲜时长 | 每个鲜度阶段时长 |
|---|---|---|
| 浆果 | 12h | 2.4h |
| 面团 | 12h | 2.4h |
| 生肉 | 24h | 4.8h |
| 蛋饼 | 36h | 7.2h |
| 蛋 | 48h | 9.6h |
| 蔬菜炖菜 | 48h | 9.6h |
| 熟肉 | 72h | 14.4h |
| 腌肉 | 120h | 24h |
| 面包 | 168h | 33.6h |
| 咸蛋饼 | 180h | 36h |
| 盐渍菜汤 | 240h | 48h |
| 浆果酱 | 720h | 144h |

补充说明：

- 木桶里的水当前没有腐烂逻辑
- 番茄果实当前没有单独配置保鲜时长，因此**不会按上表方式腐烂**

---

## 7. 液体、容器与酿酒

### 7.1 液体容器

液体只存在于"液体容器"类物品里（`kind=container` + tag `liquid_container`）。当前三种：

| 物品 | 容量 | 可堆叠 | 备注 |
|---|---|---|---|
| 木桶 wood_bucket | 20 升 | 否 | 通用打水 / 装液体 |
| 酿酒桶 brewing_barrel | 100 升 | 否 | 兼发酵容器（额外带 tag `brewing_vessel`）|
| 杯子 cup | 小 | 否 | 分装、喝酒 |

每个容器槽的液体状态 = `container_amount`(升) + `container_content`(装的是什么) + 复用 `quality` 作液体品质。倒/灌液体按量加权平均品质（核心原语在 `src/sim/items/liquid_ops.gd`）。

### 7.2 水井 = 无限液体源

水井不是传统工作台面板，而是一种**无限液体源容器**（`ContainerNode` 带 `infinite_content="water"`）：取水不减量、不占 slot；但打水动作本身会按尝试升数线性消耗游戏时间与体力。

玩家打水：靠近水井 → **鼠标悬停水井、按 `E`** → 弹出取水面板 → 列出背包里可装水的容器，每行一个数量框（默认 = 装满该容器所需）→ 点取水。服务端权威，复用与 NPC 同一套 `WaterDrawRunner`（耗时 / 体力 / 醉病缩水）和 `LiquidOps.fill_from_source`（液体入桶原语）。

> 多个可 `E` 交互对象挨在一起时，用鼠标悬停指定操作哪个（与地面物品 / NPC 同一范式，`InteractionController`）。

### 7.3 倒液体 / 水的消耗

- **倒液体**：右键一个装着液体的容器 → "倒出液体…" → 弹**分离面板**选目标容器 + 升数。容器间互倒要求同 `content`（或目标为空），品质按量加权平均。
- **把液体投进工作台**：液体在工作台 staging 槽里以"液体单位"存在（`1` 单位 = `1` 升）。
  - **拖**满液体的桶进工作台 = 倒入**全部**液体；**右键**桶（背包打开工作台时）→ 分离面板选**升数**倒入。倒出即从原桶扣量（写 `container_amount`，桶留在背包）。
  - 制造每次消耗配方所需的量，**剩余液体在关面板/取出时按原桶地址原路倒回**（见 §8.2 原路退回），绝不混进别的桶（防止稀释不同品质的酒）。
- **水的消耗**：烹饪 / 调和（把装水的桶投进工作台、配方按需扣水）；农田浇水（每次整片扣 `20` 升、整片水分恢复 `20%`，满桶 20 升正好一次）。

### 7.4 酿酒

酿酒 = 给装水的酿酒桶起头发酵，是一种**被动转化**（见 §7.5）：

1. 用水井把酿酒桶灌满水。
2. 背包备够麦芽（malt）：`1` 麦芽 : `1` 升水。
3. 右键酿酒桶 → "酿酒…" → 选原料（麦芽酒选麦芽）→ 酿造。
4. 桶里的水**立刻变成啤酒（品质 0）**，之后 `48` 小时内品质线性爬到上限，随时可取出 = 当前品质的酒。

品质上限 = `麦芽品质 × eff`，`eff = clamp(0.6 + (酿酒熟练度 - 配方难度)/100, 0, 1)`。即**麦芽品质是天花板，熟练度相对难度决定能榨出几成**。麦芽酒难度 `30`：熟练度 70 顶满麦芽品质、刚够格(30) 出六成、差很多(10) 出四成。NPC 也能酿（`brew` 工具），与玩家共用同一逻辑。

### 7.5 被动转化机制（晾晒 / 发酵）

晾晒（小麦→麦芽）和发酵（水→啤酒）都是"被动转化"：放着随时间自动变，**开始即变身、品质从 0 线性爬到上限、到时定格**。定义全在反应表 `data/mechanics/crafting.lua`（`trigger="passive"`，与主动反应同表），由全局定时器 **`PassiveSimulator`（单一写者）** 推进——每条反应自带 `tick_seconds`（酿酒 3600 / 晾晒 1800 游戏秒）+ `on_tick` 自定义逻辑。架构详见 [reaction-schema.md §8](./reaction-schema.md)。

- **晾晒**：小麦放进晾晒架（具 `drying` 能力的容器）即自动开始 → 麦芽，上限 = 小麦品质，`24` 小时到顶（80 品质小麦 12h = 40 麦芽、24h = 80 麦芽）。
- **发酵**：见 §7.4。

加一种新的被动转化（其它酒、熏制、腐烂…）= 反应表加一行，零代码。

---

## 8. 工作台与制造系统

### 8.1 当前可用工作站

| 工作站 | 数量 | 作用 |
|---|---|---|
| 工作台 | 2 | 削木、搓绳、组装工具、和面；面包店旁有专用和面点 |
| 熔炉 | 1 | 冶炼矿石、合金 |
| 铁砧 | 1 | 把金属锭打成部件 |
| 磨坊 | 2 | 把谷物磨成粉；其中一个在磨坊岗位旁 |
| 灶台 | 2 | 烘烤、混合食材；面包店旁有专用烤炉 |
| 水井 | 3 | 无限水源容器；鼠标悬停 + E 开取水面板（见 §7.2），不是工作台 |
| 金矿矿井 | 1 | 持续采金矿，需要铁镐 |
| 银矿矿井 | 1 | 持续采银矿，需要铁镐 |
| 盐锅 | 1 | 在沿海盐场把燃料熬成一批盐 |

### 8.2 输入槽位：投料、取回与原路退回

工作台输入槽位不是全都一样：

| 工作站 | 输入槽位数 |
|---|---|
| 工作台 | 5 |
| 熔炉 | 5 |
| 铁砧 | 5 |
| 灶台 | 5 |
| 磨坊 | 1 |
| 盐锅 | 1 |
| 水井 | 取水面板（非工作台面板，见 §7.2）|

**投料 / 取回**：背包 ↔ 工作台 ↔ 仓库 之间的部分量转移共用一套**分离面板**（单位随物品类型变：液体=升、离散=份数；粉末按克的容器模型暂缓，面板已留接口）。约定统一：

- **拖拽 = 全量**（拖整堆 / 倒满桶的全部）；**右键 = 开分离面板选量**（液体还要选目标容器）。
- 工作台一个 staging 槽 = 一个配方输入实例。液体存为"液体单位"（`1` 单位 = `1` 升，见 §7.3）；离散存为份数。

**原路退回**（关面板 / 取回 / 制造中途取消时）：每个 staging 槽记住它来自背包的原槽地址，统一原路退回——

- **液体** → 精确倒回**原桶**（按原桶地址，绝不混进别的桶，防稀释不同品质）。
- **离散** → 回**原槽**；原槽放不下（被占/不可堆叠）则找其它可堆叠槽，再兜底空槽。
- 放不下 / 原桶失效 → 报错并把物品留在工作台，绝不丢、绝不乱倒（fail-loud）。

实现：服务端权威（`Player.request_stage_to_workstation` / `request_unstage_*` / `_return_staged_slot`），液体原语复用 `LiquidOps`；UI 为 `src/ui/split/split_panel.gd`（统一分离面板）。

### 8.3 制作时间

所有制作时间都按**游戏秒**计时，所以会随时间倍率一起加速。

换算关系：

- `70` 游戏秒 ≈ `10` 现实秒
- `140` 游戏秒 ≈ `20` 现实秒
- `210` 游戏秒 ≈ `30` 现实秒
- `420` 游戏秒 ≈ `60` 现实秒

### 8.4 当前版本的成功率

虽然每条配方都写了 `difficulty` 和失败文案，但当前所有现有配方都满足：

- 玩家 mastery 固定为 `1.0`
- 所有配方 difficulty 都不超过 `0.3`

所以**当前版本的实际制作失败率为 0%**。

你会在数据里看到失败描述，但正常玩家在这一版里做不出失败。

### 8.5 当前版本的体力消耗

每条配方也写了名义上的体力消耗值；当前版本普通制作还不会真正扣玩家体力，采矿和农事例外。

采矿一次工具调用仍覆盖 `1` 游戏小时，用来减少 Agent 频繁调用；Godot server 内部按真实尝试结算：每 `5` 游戏分钟进行一次产出判定，每次先消耗 `10` 点体力，体力不足会提前结束。

农事按动作结算体力；体力不足时当前 op 失败，队列停止并把剩余 op 回传为 `remaining`。

| 农事动作 | 体力消耗 | 游戏时间 |
|---|---:|---:|
| 种植 | `3` | `1` 游戏分钟 |
| 收获 | `3` | `1` 游戏分钟 |
| 铲除 | `3` | `2` 游戏分钟 |
| 除虫 | `3` | `2` 游戏分钟 |
| 浇整片田 | `10` | `15` 游戏分钟 |

所以现在的制作成本主要是：

- 时间
- 原材料
- 容器里的水
- 采矿 / 农事消耗的体力

### 8.6 批量投入的品质惩罚

如果你把同一种输入一次塞太多，产物品质会被压低。

默认数量曲线如下：

| 某一输入被判定为投入了几份 | 品质乘数 |
|---|---|
| 1 份 | 1.00 |
| 2 份 | 0.70 |
| 3 份 | 0.40 |
| 4 份 | 0.15 |
| 5 份及以上 | 0.00 |

这个规则的结果是：

- 多放材料不一定更赚
- 对很多配方来说，最稳的做法仍然是按标准份数做

### 8.7 当前全部配方

#### 工作台

| 动作 | 输入 | 输出 | 游戏秒 | 约现实时间 |
|---|---|---|---|---|
| 削木杆 | 木材 x1 | 木杆 x1 | 140 | 20 秒 |
| 削木板 | 木材 x1 | 木板 x1 | 140 | 20 秒 |
| 搓绳 | 纤维 x1 | 绳 x1 | 70 | 10 秒 |
| 组装铁刀 | 铁刃 x1 + 木杆 x1 | 铁刀 x1 | 70 | 10 秒 |
| 组装镰刀 | 铁刃 x1 + 木杆 x1 | 镰刀 x1 | 84 | 12 秒 |
| 组装铁锹 | 铁刃 x1 + 木杆 x1 + 绳 x1 | 铁铲 x1 | 105 | 15 秒 |
| 组装铁镐 | 铁镐头 x1 + 木杆 x1 + 绳 x1 | 铁镐 x1 | 105 | 15 秒 |
| 组装铁斧 | 铁斧头 x1 + 木杆 x1 + 绳 x1 | 铁斧 x1 | 105 | 15 秒 |
| 和面 | 面粉 x1 + 水 x1 | 面团 x1 | 70 | 10 秒 |

#### 伐木台

| 动作 | 输入 | 输出 | 游戏秒 | 约现实时间 |
|---|---|---|---|---|
| 砍木 | 斧头（工具） | 木材 x1 | 600 | 86 秒 |

#### 熔炉

| 动作 | 输入 | 输出 | 游戏秒 | 约现实时间 |
|---|---|---|---|---|
| 冶炼矿石 | 任意矿石 x1 + 燃料 x1 | 对应金属锭 x1 | 210 | 30 秒 |
| 炼青铜 | 铜锭 x1 + 锡锭 x1 | 青铜锭 x1 | 280 | 40 秒 |

#### 铁砧

铁砧 `shape` 当前只消耗金属锭；燃料在熔炉冶炼阶段消耗，不要在锻造输入里再加入木炭或木头。

| 动作 | 输入 | 输出 | 游戏秒 | 约现实时间 |
|---|---|---|---|---|
| 打铁刃 | 任意金属锭 x1 | 铁刃 / 对应材质刃 x1 | 210 | 30 秒 |
| 打镐头 | 任意金属锭 x1 | 镐头 x1 | 210 | 30 秒 |
| 打斧头 | 任意金属锭 x1 | 斧头 x1 | 210 | 30 秒 |

#### 磨坊

| 动作 | 输入 | 输出 | 游戏秒 | 约现实时间 |
|---|---|---|---|---|
| 研磨 | 谷物 x1 | 面粉 x1 | 140 | 20 秒 |

#### 晾晒架

| 动作 | 输入 | 输出 | 游戏秒 | 约现实时间 |
|---|---|---|---|---|
| 留番茄种 | 番茄 x1 | 番茄种子 x2 | 180 | 约 26 秒 |
| 留亚麻种 | 亚麻束 x1 | 亚麻种子 x2 | 180 | 约 26 秒 |
| 取亚麻纤维 | 亚麻束 x1 | 纤维 x3 | 240 | 约 34 秒 |

> 此外，**小麦放进晾晒架会被动晾成麦芽**（品质渐变、24h，见 §7.5），是 `trigger=passive` 的反应，不占上面这些主动动作。

#### 灶台

| 动作 | 输入 | 输出 | 游戏秒 | 约现实时间 |
|---|---|---|---|---|
| 烤面包 | 面团 x1 | 面包 x1 | 175 | 25 秒 |
| 烤熟肉 | 生肉 x1 | 熟肉 x1 | 245 | 35 秒 |
| 烤腌肉 | 生肉 x1 + 盐 x1 | 腌肉 x1 | 210 | 30 秒 |
| 烤蛋饼 | 蛋 x1 | 蛋饼 x1 | 140 | 20 秒 |
| 烤咸蛋饼 | 蛋 x1 + 盐 x1 | 咸蛋饼 x1 | 154 | 22 秒 |
| 熬果酱 | 浆果 x1 + 水 x1 | 浆果酱 x1 | 420 | 60 秒 |
| 炖蔬菜汤 | 整果类原料 x1 + 水 x1 | 蔬菜炖菜 x1 | 210 | 30 秒 |
| 炖盐渍菜汤 | 整果类原料 x1 + 水 x1 + 盐 x1 | 盐渍菜汤 x1 | 245 | 35 秒 |

#### 盐场

| 动作 | 输入 | 输出 | 游戏秒 | 约现实时间 |
|---|---|---|---|---|
| 熬盐 | 燃料 x1 | 盐 x20 | 420 | 60 秒 |

### 8.8 最短成型路线示例

#### 铁锹路线

1. 铁矿石 + 木炭 → 铁锭
2. 铁锭 → 铁刃
3. 木材 → 木杆
4. 亚麻束 → 纤维（晾晒架）
5. 纤维 → 绳
6. 铁刃 + 木杆 + 绳 → 铁铲

#### 面包路线

1. 预留一部分小麦作下轮播种
2. 小麦 → 面粉
3. 面粉 + 水 → 面团
4. 面团 → 面包

#### 腌肉路线

1. 生肉 + 盐 → 腌肉

---

## 9. 农场系统

### 9.1 当前农田规模

当前城镇里共有：

- 农田 `10` 片
- 农田格 `303` 格

农田大致分布在：

- `Greystone` 区 5 片
- `Saint Bell` 区 2 片
- `North Wall` 区 3 片

### 9.2 当前正式作物

当前正式接入的作物有三种：

| 作物 | 种植物品 | 成熟时间 | 基础收成 | 收获物 |
|---|---|---|---|---|
| 小麦 | `wheat` | `72` 游戏小时 | `4` | `wheat` |
| 亚麻 | `flax_seed` | `72` 游戏小时 | `3` | `flax_bundle` |
| 番茄 | `tomato_seed` | `80` 游戏小时 | `3` | `tomato_fruit` |

种子信息：

| 物品 | 类型 | 最大堆叠 | 对应作物 |
|---|---|---|---|
| 番茄种子 | seed | 99 | tomato |
| 亚麻种子 | seed | 99 | flax |

小麦信息：

| 物品 | 类型 | 最大堆叠 | 对应作物 |
|---|---|---|---|
| 小麦 | material | 99 | wheat |

### 9.3 当前作物生长时间

小麦和亚麻完整生长周期的内部时钟长度：

- `72` 游戏小时
- 约 `10 小时 17 分` 现实时间

小麦和亚麻共有 5 个阶段，每 `14.4` 游戏小时推进 1 段：

| 阶段 | 开始时间 |
|---|---|
| 种子 | 0h |
| 发芽 | 14.4h |
| 生长 | 28.8h |
| 开花 | 43.2h |
| 成熟 | 57.6h |

番茄完整生长周期的内部时钟长度：

- `80` 游戏小时
- 约 `11 小时 26 分` 现实时间

番茄共有 5 个阶段，每 `16` 游戏小时推进 1 段：

| 阶段 | 开始时间 |
|---|---|
| 种子 | 0h |
| 发芽 | 16h |
| 生长 | 32h |
| 开花 | 48h |
| 成熟 | 64h |

更直观地说：

- 大约过 `16` 小时会看到一次明显长势变化
- 到 `64` 小时就能收
- `80` 小时是整段成熟周期的内部时钟长度上限

### 9.4 水分规则

农田初始水分是 `60%`（`0.6`）。

每游戏小时：

- 水分 `-0.05`

这意味着：

- 完全不浇水时，约 `20` 游戏小时会从满水掉到 0
- 当水分低于 `0.2` 时，这一小时会被视为“缺水”

换算一下：

- 从满水 `1.0` 掉到缺水阈值 `0.2`，需要约 `16` 游戏小时

### 9.5 虫害规则

虫害只会在作物进入“生长”阶段以后开始出现。

以每片农田为单位：

- 每游戏小时触发虫害的概率：`4%`
- 每游戏日最多触发虫害：`1` 次

这意味着：

- 虫害不是每株独立乱跳，而是按整片农田限频
- 巡田频率大于“每天一次”时，通常就能稳住局面

### 9.6 照料评分与品质

作物品质不是由成熟时间单独决定，而是由整个生长期的照料情况决定。

每个游戏小时都会结算一次“这一小时照料得好不好”：

- 水分不低于 `0.2`：`+0.5` 分
- 当前没有虫害：`+0.5` 分

所以每小时最高得分是 `1.0`，最低是 `0.0`。

整株作物最后会把所有小时分数求平均，再换算成 `1-100` 的品质。

品质区间显示为：

| 品质 | 文本显示 |
|---|---|
| 1-39 | 差 |
| 40-69 | 中 |
| 70-89 | 良 |
| 90-100 | 优 |

### 9.7 水分最佳区间

作物当前的最佳生长水分带为 `20% - 80%`：

- 低于 `20%` 会因为缺水损失照料分
- 高于 `80%` 会因为过湿损失照料分

也就是说，浇水现在不是“越满越好”，而是要把整片田尽量维持在最佳区间内。

### 9.8 收获规则

小麦、亚麻和番茄当前为**单次收获作物**：

- 成熟后可收
- 收完后植株会直接消失
- 小麦收获物仍然是 `wheat` 本身
- 亚麻收获物是 `flax_bundle`，需要去晾晒架选择留种或取纤维
- 番茄收获物是 `tomato_fruit`，需要去晾晒架留种才会得到 `tomato_seed`

这意味着小麦现在同时承担：

- 播种粮
- 原粮
- 磨粉原料

小麦基础产量：

- `4` 份小麦

亚麻基础产量：

- `3` 份亚麻束

亚麻束后续在晾晒架处理：

- `dry/save_seed`：`flax_bundle x1 -> flax_seed x2`
- `dry/fiber`：`flax_bundle x1 -> fiber x3`

实际收获量公式：

- `每种产物的基础数量 x 品质倍率`
- 然后四舍五入
- 最少保底 `1` 个

举例：

| 品质 | 倍率 | 实际数量 |
|---|---|---|
| 100 | 1.00 | 4 |
| 70 | 0.70 | 3 |
| 40 | 0.40 | 2 |
| 1 | 0.01 | 1 |

番茄当前为**单次收获作物**：

- 成熟后可收
- 收完后植株会直接消失
- 不会进入第二轮结果

当前番茄的主要用途是：

- 直接食用

它暂时**没有接进现有炖菜 / 果酱配方链**。

番茄基础产量：

- `3` 个番茄

番茄果实后续在晾晒架处理：

- `dry/save_seed`：`tomato_fruit x1 -> tomato_seed x2`

实际收获量公式：

- `3 x 品质倍率`
- 然后四舍五入
- 最少保底 `1` 个

举例：

| 品质 | 倍率 | 实际数量 |
|---|---|---|
| 100 | 1.00 | 3 |
| 70 | 0.70 | 2 |
| 40 | 0.40 | 1 |
| 1 | 0.01 | 1 |

### 9.9 农事动作耗时与消耗

| 动作 | 游戏时间 | 约现实时间 | 消耗 / 要求 |
|---|---|---|---|
| 打水 | 20 升 = 3 游戏分钟，按升线性 | 20 升约 25.7 秒 | 完成时消耗体力 `3 / 20L`，按尝试升数线性；醉酒/生病会降低实际入桶水量 |
| 种植 | 1 游戏分钟 | 约 8.6 秒 | 完成时消耗体力 `3`；消耗 1 份可种植物（如 `wheat` 或 `tomato_seed`） |
| 除虫 | 2 游戏分钟 | 约 17.1 秒 | 完成时消耗体力 `3`；消耗 1 份草木灰 |
| 浇水整片 | 15 游戏分钟 | 约 2 分 9 秒 | 完成时消耗体力 `10`；消耗 20 单位桶水 |
| 收获 | 1 游戏分钟 | 约 8.6 秒 | 完成时消耗体力 `3`；无额外工具要求 |
| 铲除 | 2 游戏分钟 | 约 17.1 秒 | 完成时消耗体力 `3`；需要背包里有铁铲并消耗耐久 |

### 9.10 当前农具实际作用

| 物品 | 当前实际作用 |
|---|---|
| 木桶 | 打水、浇水、参与烹饪 |
| 草木灰 | 除虫时消耗 |
| 铁铲 | 铲除作物时必须携带，并消耗耐久 |
| 镰刀 | 当前没有被收获流程强制要求 |

所以这版里：

- 收获番茄**不需要镰刀**
- 镰刀已经能做出来，但还不是强制农具

### 9.11 农田面板与批量处理

靠近农田后可以打开农场面板，对一整片地规划动作。

当前支持的批量动作：

- 种植
- 除虫
- 收获
- 铲除
- 浇水整片

队列执行特点：

- 角色会一个个格子走过去处理
- 中途被打断时，会留下“已完成 / 未完成”两部分结果
- 如果桶中途没水，浇水会停在没浇到的地方

---

## 10. NPC 与自然语言指令

### 10.1 普通说话

聊天框里直接输入普通文字，会被当成“对附近说话”。

当前默认是近距离喊话：

- `near` 半径：`2.0`
- `far` 半径：`4.0`

### 10.2 `/command`

输入 `/command 你的句子` 后：

1. 文本会被发给后台
2. 后台代理会解析成意图
3. 你的角色按意图去移动、交互或执行动作

当前比较适合的用法：

- `/command 前往铁匠铺`
- `/command 去最近的农田`
- `/command 去磨坊做面粉`

### 10.3 NPC 的当前定位

这一版里的 NPC 主要承担“小镇生活体”的作用：

- 他们有固定身份和分组
- 农户组默认带番茄种子、小麦、铁铲、草木灰和木桶
- 磨坊主带小麦、面粉和镰刀；面包师带面粉、面团、木桶、厨房刀和面包
- 磨坊与面包店各有一个货架，可通过 update_shelf / view_shelf / buy_from_shelf 参与交易
- 他们已经和农场 / 地点 / 后台上下文连在一起

现阶段更适合把 NPC 理解成：

- 世界里会说话、会被感知、会被指令系统引用的角色

而不是完整开放式任务树 NPC。

---

## 11. 当前版本最值得记住的规则

如果只记这版最关键的几条，优先记下面这些：

1. 新角色开局有 `1` 个木桶，但**没有水**，第一件事通常是去井边打水。
2. 食物必须用 `/eat <slot>` 才会恢复数值，右键“使用”不会触发食物效果。
3. 饱食会自然下降，体力会按饱食 / 精力上限自然恢复；普通制作目前仍不会扣体力，农事和采矿会扣。
4. 所有现有工作台配方在当前版本里**实际失败率为 0%**。
5. 当前正式作物有小麦和番茄：小麦 `57.6` 游戏小时进入可收获阶段、基础产量 `4`；番茄 `64` 游戏小时进入可收获阶段、基础产量 `3`。
6. 浇水不是“按一次全场免费补满”，而是**整片田一次扣 20 单位桶水、10 点体力**。
7. 除虫要消耗草木灰，铲除要带铁铲，收获番茄当前不要求镰刀。
8. 会腐烂的食物已经接上了，囤货时最耐放的是浆果酱，其次是盐渍菜汤、咸蛋饼、面包。

---

## 12. 一页版路线建议

如果你第一次进当前版本，最实用的开局路线通常是：

1. 去水井把木桶装满
2. 到磨坊把小麦磨成面粉
3. 在工作台把面粉和水和成面团
4. 到灶台烤出第一批面包
5. 有余力时继续走铁矿石 → 铁锭 → 铁刃 → 木杆 / 绳 → 铁锹路线
6. 留一部分小麦作播种粮，或带着番茄种子去农田种下第一轮作物

这条路线能一次性体验到：

- 容器与水
- 基础食物链
- 基础工具链
- 农场系统
